spring.profiles.include: db-local

debug: true

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging.level.org.hibernate.type.descriptor.sql: trace
